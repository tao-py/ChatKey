开发一款工具，工具添加可访问的网页链接，一般为AI问答网页，如DeepSeek、豆包、千问、文心一言

在工具中输入提问内容，工具访问网页在对话框中自动输入提问内容，等待网页自动返回网页回答内容

可对同一个提问内容，访问多个AI问答网页，返回回答内容对比展示以供用户选择

工具可将返回的网页回答内容格式转化为，调用大模型开发平台api_key返回的相同格式，提供特定的本地服务接口，生成本地api_key，内容存储在本地本地服务上，以供opencode、openclaw等工具调用本地的api_key。其中本地服务接口，生成本地api_key用户可以自定义
